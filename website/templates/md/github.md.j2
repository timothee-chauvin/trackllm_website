{% import "_base.md.j2" as b %}
{{ b.header("TrackLLM on GitHub", machine, nav_prefix) }}
{{ b.crumb([("Home", nav_prefix ~ "index.md"), ("GitHub", "")]) }}
TrackLLM is fully open-source (code and data).

{{ b.repo_links() }}
{{ b.footer(nav_prefix) }}
