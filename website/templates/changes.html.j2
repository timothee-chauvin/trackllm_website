{% extends "base.html.j2" %}
{% from "_macros.html.j2" import chip, search_box %}

{% block title %}Changes | TrackLLM{% endblock %}

{% block content %}
<div class="wrap">
  <div class="head">
    <div>
      <h1>Detected changes</h1>
      <p class="lede" id="lede"></p>
      <div class="summary" id="summary"></div>
    </div>
  </div>
</div>

<main class="wrap">
  <section class="block">
    <div class="sec-head">
      <h2>Most-changed endpoints</h2>
      <span class="hint">the <a href="endpoints.html">endpoint directory</a> sorts the same field</span>
    </div>
    <div class="board" id="topEndpoints"></div>
  </section>

  <section class="block">
    <div class="sec-head"><h2>Log</h2><span class="hint" id="logCount"></span></div>
    <div class="toolbar">
      {{ search_box("q", "Search model, provider, or org…", "Search changes by model, provider, or org") }}
      <div class="chips" id="chips">
        {{ chip("LT", "f", "lt") }}
        {{ chip("B3IT", "f", "b3it") }}
        {{ chip("Last 90 days", "f", "recent", state="state-changed") }}
        {{ chip("Large drift", "f", "big", state="state-changed") }}
      </div>
    </div>
    <div class="feed" id="log"></div>
  </section>
</main>

<div class="note">Magnitude is the drift level reached after the changepoint — nats for LT, total variation for B3IT. σ is one of the metrics feeding LT's change-detection criterion, not a size measure: a change can score high on σ and still be behaviorally small.</div>
{% endblock %}

{% block scripts %}
    <script type="module" src="js/changes.js?v={{ ASSET_V }}"></script>
{% endblock %}
