// PLACEHOLDER — Task 9 replaces this file with the change-log frontend.
// It exists so `bun build` can resolve the `src/changes.ts` entrypoint that
// package.json declares and `changes.html` loads as `js/changes.js`.
export {};
