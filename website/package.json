{
  "name": "trackllm-website",
  "private": true,
  "scripts": {
    "build": "bun build src/endpoint.ts src/spend.ts --outdir=js --minify --splitting",
    "watch": "bun build src/endpoint.ts src/spend.ts --outdir=js --watch --splitting"
  },
  "dependencies": {
    "plotly.js-dist-min": "^3.3.1"
  },
  "devDependencies": {
    "@types/plotly.js-dist-min": "^2.3.4",
    "typescript": "^5.7.0"
  }
}
