{
  "name": "trackllm-website",
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.11.0",
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.0"
  },
  "private": true,
  "scripts": {
    "build": "bun build src/endpoint.ts src/overview.ts src/model.ts src/provider.ts src/changes.ts src/tips.ts --outdir=js --minify --splitting",
    "watch": "bun build src/endpoint.ts src/overview.ts src/model.ts src/provider.ts src/changes.ts src/tips.ts --outdir=js --watch --splitting",
    "test": "bun test"
  }
}
