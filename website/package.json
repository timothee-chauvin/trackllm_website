{
  "name": "trackllm-website",
  "private": true,
  "scripts": {
    "build": "bun build src/endpoint.ts --outdir=js --minify",
    "watch": "bun build src/endpoint.ts --outdir=js --watch"
  },
  "dependencies": {
    "plotly.js-dist-min": "^3.3.1"
  },
  "devDependencies": {
    "@types/plotly.js-dist-min": "^2.3.4",
    "typescript": "^5.7.0"
  }
}
