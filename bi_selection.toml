# BI monitoring selection policy.
#
# SELECTION EMPTIED 2026-06-21 to prevent unattended onboarding spend overnight.
# With no [[rule]] blocks and exclude = ["*"], select_monitoring_targets returns
# zero endpoints, so update_endpoints_bi_lifecycle onboards/rechecks/delists nothing.
# (Catalog vetting in update_endpoints_bi still runs, but it is ~1 cheap query per
# endpoint and is cost-guarded.)
#
# Restore the real policy before re-enabling onboarding:
#   - prior committed policy: git show 774f879754:bi_selection.toml
#   - in-progress draft (qwen pattern narrowed): /home/ubuntu/bi_selection_wip_backup_2026-06-21.toml

budget_per_month = 0.0
max_endpoint_cost = 0.50

# Exclude everything: nothing is eligible for selection while emptied.
exclude = ["*"]
